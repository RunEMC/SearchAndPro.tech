import React from 'react';

export default class SingleUserFeed extends React.Component {
    render() {
        return (
            <div>
                Single User Feed
            </div>
        )
    }
}